{
  "name": "@jotster/emoji",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    "./*.js": {
      "types": "./dist/tsonic/bindings/*.d.ts",
      "default": "./dist/tsonic/bindings/*.js"
    }
  },
  "scripts": {
    "typecheck": "cd ../.. && ${TSONIC_BIN:-tsonic} restore --project emoji && cd packages/emoji && tsc -p tsconfig.json",
    "build": "cd ../.. && ${TSONIC_BIN:-tsonic} build --project emoji"
  }
}
