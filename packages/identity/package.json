{
  "name": "@jotster/identity",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "exports": {
    ".": "./src/index.ts",
    "./index.js": "./src/index.ts",
    "./Jotster.Identity.js": "./src/index.ts"
  },
  "dependencies": {
    "@jotster/core": "0.0.0"
  },
  "scripts": {
    "typecheck": "cd ../.. && ${TSONIC_BIN:-tsonic} restore --project identity && cd packages/identity && tsc -p tsconfig.json",
    "build": "cd ../.. && ${TSONIC_BIN:-tsonic} build --project identity"
  }
}
