{
  "name": "@jotster/server",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "typecheck": "cd ../.. && ${TSONIC_BIN:-tsonic} restore --project server && cd packages/server && tsc -p tsconfig.json",
    "build": "cd ../.. && rm -rf packages/server/generated/bin/Release/net10.0/linux-x64 packages/server/generated/obj/Release/net10.0/linux-x64 && ${TSONIC_BIN:-tsonic} restore --project server && ${TSONIC_BIN:-tsonic} generate --project server && ${TSONIC_BIN:-tsonic} build --no-generate --project server",
    "dev": "cd ../.. && ${TSONIC_BIN:-tsonic} restore --project server && ${TSONIC_BIN:-tsonic} generate --project server && ${TSONIC_BIN:-tsonic} run --no-generate --project server"
  }
}
